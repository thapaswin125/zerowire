name: Build and deploy Pages

on:
  push:
    branches: [main]
  # A push made by a workflow using the built-in GITHUB_TOKEN does NOT fire
  # the push trigger above, so the daily bot commits would never deploy.
  # workflow_run fires regardless, which is what keeps the site current.
  workflow_run:
    workflows: ["RSS digest", "Publish selected drafts"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Pin to main: on a workflow_run trigger the default checkout ref can
      # be the commit from before the bot pushed today's posts.
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build site
        env:
          JEKYLL_ENV: production
        run: bundle exec jekyll build

      - name: Build Pagefind search index
        run: npx --yes pagefind --site _site

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
